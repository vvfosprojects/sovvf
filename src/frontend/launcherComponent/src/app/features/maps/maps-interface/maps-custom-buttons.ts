export interface ButtonControlAnimation {
    toggleStatus: boolean;
    toggleMsg?: string;
    faIcon?: string;
    disabled?: boolean;
}

export enum CustomButtonsMaps {
    Centra,
    ToggleAnimation
}
