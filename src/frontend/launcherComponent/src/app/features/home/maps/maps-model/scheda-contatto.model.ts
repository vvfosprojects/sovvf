import { SchedaContatto } from '../../../../shared/interface/scheda-contatto.interface';

export class SchedaContattoMarker {

    constructor(
        /**
         * Contiene le proprietà della Scheda Contatto
         */
        public schedaContatto: SchedaContatto,
    ) {
    }

}
