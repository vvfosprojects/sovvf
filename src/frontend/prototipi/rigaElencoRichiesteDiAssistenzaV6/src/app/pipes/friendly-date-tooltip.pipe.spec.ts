import { FriendlyDateTooltipPipe } from './friendly-date-tooltip.pipe';

describe('FriendlyDateTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendlyDateTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
