import {FriendlyHourPipe} from './friendly-hour.pipe';

describe('FriendlyHourPipe', () => {
    it('create an instance', () => {
        const pipe = new FriendlyHourPipe();
        expect(pipe).toBeTruthy();
    });
});
